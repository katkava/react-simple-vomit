import React, { Component } from 'react';
import './Footer.css';
//footer component, importing react component  (destructuring)
//importing the css from our footer too 

class Footer extends Component {
  //need render method because we're using a class based component, this is syntax. 
  render() {
    return (
        <footer className="Footer">
        </footer>

    );
  }
}

export default Footer;
